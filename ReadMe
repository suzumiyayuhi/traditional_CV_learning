image_processing中的代码来自朱虹的《数字图像处理》中前几章的算法实现
OpenCVTest则是鲁鹏的《机器视觉》课程中，到sift为止所有算法的实现，比较麻烦，只写了sift的特征点查找
tool_for_amalsis则是集成了许多思路的一个小程序，主体是一个dll，然后通过c++和python去调用函数。

opencv版本4.5.3

因为很麻烦，所以程序中的指针大部分都没有去释放，所以用久了会内存泄漏（
还有c++中opencv的图的数据格式问题很麻烦，学成了之后没有去改动
嘛，就这样吧。